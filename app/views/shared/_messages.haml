- unless flash.blank?
  .flash-container
    .container
      - flash.each do |type, msg|
        .alert.fade.in{class: "alert-#{bootstrap_class_for(type)}"}
          %button.close{'data-dismiss' => 'alert'} ×
          = msg.html_safe